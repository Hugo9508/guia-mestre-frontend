🚀 GUIA COMPLETO - FASE 2: A LÓGICA (JavaScript)
📊 VISÃO GERAL
Antes (Atual)

✅ 5 exercícios básicos
✅ 3 vídeos
✅ 1 desafio final (Calc IMC)

Depois (Melhorado) ✨

✨ 10 exercícios progressivos (+ 5 novos)
✨ 6 vídeos educacionais (+ 3 novos)
✨ 1 jogo interativo (JavaScript Quiz)
✨ 1 mini-projeto (Calculadora Completa)
✨ 1 desafio final aprimorado (Calc IMC Avançada)
✨ Sistema de progresso visual
✨ Badges de conquistas


📚 TÓPICOS COMPLETOS DA FASE 2
🎯 Checklist de Conteúdo
1. Fundamentos JavaScript

✅ Variáveis (let, const, var)
✅ Tipos de dados (string, number, boolean, array, object)
✅ Operadores (aritméticos, lógicos, comparação)
✅ Template strings (${})

2. Lógica de Programação

✅ Condicionais (if, else, else if)
✅ Switch case
✅ Operador ternário (? :)
✅ Loops (for, while, forEach)

3. DOM Manipulation

✅ Seleção de elementos (querySelector, getElementById)
✅ Manipulação de conteúdo (textContent, innerHTML)
✅ Manipulação de estilos (classList, style)
✅ Criação de elementos (createElement, appendChild)

4. Eventos

✅ Event Listeners (click, input, submit)
✅ Event object (e.target, e.preventDefault)
✅ Eventos de teclado (keypress, keydown)
✅ Eventos de mouse (hover, mouseover)

5. ES6+ Moderno

✅ Arrow Functions (=>)
✅ Destructuring ({ }, [ ])
✅ Spread operator (...)
✅ Array Methods (map, filter, reduce, find)

6. Objetos e Arrays

✅ Criação e manipulação de objetos
✅ Métodos de arrays
✅ JSON (parse, stringify)
✅ Array de objetos


💪 EXERCÍCIOS PRÁTICOS (10 TOTAL)
✅ Exercícios Existentes (5)
🎯 Exercício 1: Variáveis e Matemática
Objetivo: Usar variáveis e operações matemáticas
Código Inicial:
javascriptfunction CalculoPreco() {
  // Defina as variáveis aqui
  const preco = 0;
  const quantidade = 0;
  const total = 0;

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>🛒 Carrinho de Compras</h3>
      <p>Preço unitário: R$ {preco}</p>
      <p>Quantidade: {quantidade}</p>
      <hr />
      <h2>Total: R$ {total}</h2>
    </div>
  );
}
Instrução:
Calcule o total multiplicando preço × quantidade. Use preco = 50 e quantidade = 3.
Dica: total = preco * quantidade

🎯 Exercício 2: Condicional (If/Else)
Objetivo: Mostrar mensagens diferentes baseado em condição
Código Inicial:
javascriptfunction StatusUsuario() {
  const isOnline = true; // Mude para false para testar

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>Status do Usuário</h3>
      {/* Use if/else aqui */}
      <p>Status: ?</p>
    </div>
  );
}
Instrução:
Mostre "🟢 Online" em verde se isOnline for true, ou "🔴 Offline" em vermelho se for false.
Dica: Use operador ternário: {isOnline ? "Online" : "Offline"}

🎯 Exercício 3: Arrays e .map()
Objetivo: Transformar array em lista HTML
Código Inicial:
javascriptfunction ListaDeTarefas() {
  const tarefas = ['Estudar React', 'Fazer exercícios', 'Praticar JavaScript'];

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>📝 Minhas Tarefas</h3>
      <ul>
        {/* Use .map() aqui para criar <li> */}
      </ul>
    </div>
  );
}
Instrução:
Use o método .map() para transformar cada item do array em um elemento <li>.
Dica: {tarefas.map((tarefa, index) => <li key={index}>{tarefa}</li>)}

🎯 Exercício 4: Objetos
Objetivo: Criar e exibir dados de um objeto
Código Inicial:
javascriptfunction PerfilUsuario() {
  const usuario = {
    nome: '',
    idade: 0,
    cidade: '',
    profissao: ''
  };

  return (
    <div style={{ 
      padding: '20px', 
      fontFamily: 'sans-serif',
      border: '2px solid #e5e7eb',
      borderRadius: '8px',
      maxWidth: '300px'
    }}>
      <h3>👤 Perfil</h3>
      {/* Exiba os dados do usuário aqui */}
    </div>
  );
}
Instrução:
Preencha o objeto usuário com seus dados e exiba-os usando {usuario.nome}, etc.
Dica: Acesse propriedades com ponto: usuario.nome, usuario.idade

🎯 Exercício 5: Eventos (onClick)
Objetivo: Criar interatividade com eventos
Código Inicial:
javascriptfunction BotaoInterativo() {
  const handleClick = () => {
    // Adicione um alert aqui
  };

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <button 
        style={{
          padding: '12px 24px',
          backgroundColor: '#3b82f6',
          color: 'white',
          border: 'none',
          borderRadius: '6px',
          cursor: 'pointer'
        }}
      >
        Clique em mim!
      </button>
    </div>
  );
}
Instrução:
Adicione um alert("Você clicou!") dentro de handleClick e conecte a função ao botão.
Dica: Use onClick={handleClick} no botão

✨ NOVOS EXERCÍCIOS (5 adicionais)
🎯 Exercício 6: Filtrar Array
Objetivo: Buscar nomes em uma lista
Código Inicial:
javascriptfunction BuscaNomes() {
  const [busca, setBusca] = React.useState('');
  
  const nomes = [
    'Ana Silva',
    'Bruno Costa',
    'Carlos Santos',
    'Diana Oliveira',
    'Eduardo Lima',
    'Fernanda Souza'
  ];

  // Filtre os nomes aqui
  const nomesFiltrados = nomes;

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>🔍 Buscar Pessoa</h3>
      <input
        type="text"
        placeholder="Digite um nome..."
        value={busca}
        onChange={(e) => setBusca(e.target.value)}
        style={{
          padding: '10px',
          width: '100%',
          marginBottom: '20px',
          border: '1px solid #d1d5db',
          borderRadius: '6px'
        }}
      />
      <div>
        {nomesFiltrados.length === 0 ? (
          <p style={{ color: '#6b7280' }}>Nenhum resultado encontrado</p>
        ) : (
          <ul style={{ listStyle: 'none', padding: 0 }}>
            {nomesFiltrados.map((nome, i) => (
              <li 
                key={i}
                style={{
                  padding: '10px',
                  backgroundColor: '#f3f4f6',
                  marginBottom: '8px',
                  borderRadius: '6px'
                }}
              >
                {nome}
              </li>
            ))}
          </ul>
        )}
      </div>
    </div>
  );
}
Instrução:
Use .filter() para mostrar apenas nomes que contenham o texto digitado na busca (toLowerCase para ignorar maiúsculas).
Dica: nomes.filter(nome => nome.toLowerCase().includes(busca.toLowerCase()))
Dificuldade: ⭐⭐ Médio | Tempo: 15 min

🎯 Exercício 7: Timer/Cronômetro
Objetivo: Criar um cronômetro simples
Código Inicial:
javascriptfunction Cronometro() {
  const [segundos, setSegundos] = React.useState(0);
  const [ativo, setAtivo] = React.useState(false);

  React.useEffect(() => {
    let intervalo = null;
    
    if (ativo) {
      intervalo = setInterval(() => {
        setSegundos(s => s + 1);
      }, 1000);
    } else {
      clearInterval(intervalo);
    }
    
    return () => clearInterval(intervalo);
  }, [ativo]);

  const toggleTimer = () => {
    setAtivo(!ativo);
  };

  const resetTimer = () => {
    setSegundos(0);
    setAtivo(false);
  };

  const formatarTempo = (seg) => {
    const mins = Math.floor(seg / 60);
    const secs = seg % 60;
    return `${mins.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`;
  };

  return (
    <div style={{ 
      padding: '40px', 
      fontFamily: 'sans-serif',
      textAlign: 'center'
    }}>
      <h3>⏱️ Cronômetro</h3>
      <div style={{
        fontSize: '48px',
        fontWeight: 'bold',
        margin: '20px 0',
        color: '#1f2937'
      }}>
        {formatarTempo(segundos)}
      </div>
      <div style={{ display: 'flex', gap: '10px', justifyContent: 'center' }}>
        <button 
          onClick={toggleTimer}
          style={{
            padding: '12px 24px',
            backgroundColor: ativo ? '#ef4444' : '#10b981',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            cursor: 'pointer',
            fontWeight: 'bold'
          }}
        >
          {ativo ? '⏸️ Pausar' : '▶️ Iniciar'}
        </button>
        <button 
          onClick={resetTimer}
          style={{
            padding: '12px 24px',
            backgroundColor: '#6b7280',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            cursor: 'pointer'
          }}
        >
          🔄 Resetar
        </button>
      </div>
    </div>
  );
}
Instrução:
O cronômetro já está funcional! Personalize:

Mude as cores dos botões
Adicione um botão de "Lap" (marcar tempo)
Adicione um som quando chegar em 1 minuto

Dica: Para lap, crie um array de tempos marcados com useState
Dificuldade: ⭐⭐ Médio | Tempo: 20 min

🎯 Exercício 8: Gerador de Cor Aleatória
Objetivo: Gerar cores hexadecimais aleatórias
Código Inicial:
javascriptfunction GeradorDeCores() {
  const [cor, setCor] = React.useState('#3b82f6');

  const gerarCorAleatoria = () => {
    // Crie uma cor hex aleatória aqui
    const novaCor = '#000000';
    setCor(novaCor);
  };

  return (
    <div style={{ 
      padding: '40px', 
      fontFamily: 'sans-serif',
      textAlign: 'center'
    }}>
      <div
        style={{
          width: '200px',
          height: '200px',
          backgroundColor: cor,
          margin: '0 auto 20px',
          borderRadius: '12px',
          boxShadow: '0 4px 6px rgba(0,0,0,0.1)',
          transition: 'all 0.3s ease'
        }}
      />
      <h3 style={{ 
        fontFamily: 'monospace',
        fontSize: '24px',
        color: '#1f2937'
      }}>
        {cor}
      </h3>
      <button
        onClick={gerarCorAleatoria}
        style={{
          padding: '12px 24px',
          backgroundColor: '#1f2937',
          color: 'white',
          border: 'none',
          borderRadius: '6px',
          cursor: 'pointer',
          marginTop: '20px',
          fontWeight: 'bold'
        }}
      >
        🎨 Gerar Nova Cor
      </button>
    </div>
  );
}
Instrução:
Gere uma cor hexadecimal aleatória (#RRGGBB). Use Math.random() para gerar valores de 0-255 e converta para hex.
Dica:
javascriptconst r = Math.floor(Math.random() * 256).toString(16).padStart(2, '0');
const g = Math.floor(Math.random() * 256).toString(16).padStart(2, '0');
const b = Math.floor(Math.random() * 256).toString(16).padStart(2, '0');
const novaCor = `#${r}${g}${b}`;
Dificuldade: ⭐⭐ Médio | Tempo: 15 min

🎯 Exercício 9: Validação de Formulário
Objetivo: Validar campos antes de enviar
Código Inicial:
javascriptfunction FormularioValidado() {
  const [nome, setNome] = React.useState('');
  const [email, setEmail] = React.useState('');
  const [senha, setSenha] = React.useState('');
  const [erros, setErros] = React.useState({});

  const validarFormulario = () => {
    const novosErros = {};

    // Adicione validações aqui
    if (nome.trim() === '') {
      novosErros.nome = 'Nome é obrigatório';
    }

    // TODO: Validar email e senha

    setErros(novosErros);
    return Object.keys(novosErros).length === 0;
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    
    if (validarFormulario()) {
      alert('✅ Formulário válido! Dados: ' + JSON.stringify({ nome, email, senha }));
    }
  };

  return (
    <div style={{ 
      padding: '20px', 
      maxWidth: '400px',
      fontFamily: 'sans-serif'
    }}>
      <h3>📝 Cadastro</h3>
      <form onSubmit={handleSubmit}>
        <div style={{ marginBottom: '15px' }}>
          <label style={{ display: 'block', marginBottom: '5px', fontWeight: 'bold' }}>
            Nome:
          </label>
          <input
            type="text"
            value={nome}
            onChange={(e) => setNome(e.target.value)}
            style={{
              width: '100%',
              padding: '10px',
              border: erros.nome ? '2px solid #ef4444' : '1px solid #d1d5db',
              borderRadius: '6px'
            }}
          />
          {erros.nome && (
            <span style={{ color: '#ef4444', fontSize: '12px' }}>{erros.nome}</span>
          )}
        </div>

        <div style={{ marginBottom: '15px' }}>
          <label style={{ display: 'block', marginBottom: '5px', fontWeight: 'bold' }}>
            Email:
          </label>
          <input
            type="email"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            style={{
              width: '100%',
              padding: '10px',
              border: erros.email ? '2px solid #ef4444' : '1px solid #d1d5db',
              borderRadius: '6px'
            }}
          />
          {erros.email && (
            <span style={{ color: '#ef4444', fontSize: '12px' }}>{erros.email}</span>
          )}
        </div>

        <div style={{ marginBottom: '15px' }}>
          <label style={{ display: 'block', marginBottom: '5px', fontWeight: 'bold' }}>
            Senha:
          </label>
          <input
            type="password"
            value={senha}
            onChange={(e) => setSenha(e.target.value)}
            style={{
              width: '100%',
              padding: '10px',
              border: erros.senha ? '2px solid #ef4444' : '1px solid #d1d5db',
              borderRadius: '6px'
            }}
          />
          {erros.senha && (
            <span style={{ color: '#ef4444', fontSize: '12px' }}>{erros.senha}</span>
          )}
        </div>

        <button
          type="submit"
          style={{
            width: '100%',
            padding: '12px',
            backgroundColor: '#3b82f6',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            cursor: 'pointer',
            fontWeight: 'bold'
          }}
        >
          Cadastrar
        </button>
      </form>
    </div>
  );
}
Instrução:
Complete as validações:

Nome: mínimo 3 caracteres
Email: deve conter @ e .
Senha: mínimo 6 caracteres

Dica:

Email: email.includes('@') && email.includes('.')
Senha: senha.length >= 6

Dificuldade: ⭐⭐⭐ Difícil | Tempo: 25 min

🎯 Exercício 10: Accordion/FAQ
Objetivo: Criar seções que abrem/fecham
Código Inicial:
javascriptfunction Accordion() {
  const [abaAberta, setAbaAberta] = React.useState(null);

  const perguntas = [
    {
      id: 1,
      pergunta: 'O que é JavaScript?',
      resposta: 'JavaScript é uma linguagem de programação que torna as páginas web interativas.'
    },
    {
      id: 2,
      pergunta: 'O que é React?',
      resposta: 'React é uma biblioteca JavaScript para construir interfaces de usuário.'
    },
    {
      id: 3,
      pergunta: 'O que é uma variável?',
      resposta: 'Uma variável é um espaço na memória para armazenar dados.'
    }
  ];

  const toggleAba = (id) => {
    // Se a aba já está aberta, fecha. Senão, abre.
    setAbaAberta(abaAberta === id ? null : id);
  };

  return (
    <div style={{ 
      padding: '20px', 
      maxWidth: '600px',
      fontFamily: 'sans-serif'
    }}>
      <h3>❓ Perguntas Frequentes</h3>
      {perguntas.map((item) => (
        <div 
          key={item.id}
          style={{
            marginBottom: '10px',
            border: '1px solid #e5e7eb',
            borderRadius: '8px',
            overflow: 'hidden'
          }}
        >
          <button
            onClick={() => toggleAba(item.id)}
            style={{
              width: '100%',
              padding: '15px',
              backgroundColor: abaAberta === item.id ? '#3b82f6' : '#f3f4f6',
              color: abaAberta === item.id ? 'white' : '#1f2937',
              border: 'none',
              textAlign: 'left',
              cursor: 'pointer',
              fontWeight: 'bold',
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center'
            }}
          >
            {item.pergunta}
            <span>{abaAberta === item.id ? '▲' : '▼'}</span>
          </button>
          {abaAberta === item.id && (
            <div style={{
              padding: '15px',
              backgroundColor: 'white',
              borderTop: '1px solid #e5e7eb'
            }}>
              {item.resposta}
            </div>
          )}
        </div>
      ))}
    </div>
  );
}
Instrução:
O accordion já funciona! Melhore:

Adicione animação de entrada (transition)
Mude o ícone para + e - ao invés de setas
Adicione mais perguntas ao array

Dica: Para animação suave, use CSS transition: 'all 0.3s ease'
Dificuldade: ⭐⭐ Médio | Tempo: 20 min

🎥 RECURSOS EDUCACIONAIS
Vídeos Recomendados (6 total)

📹 Curso Lógica de Programação - Guanabara

URL: https://www.youtube.com/playlist?list=PLHz_AreHm4dlsK3Nr9GVvXCbpQyHQl1o1
Duração: ~40 aulas
Nível: Iniciante
Descrição: Fundamentos de lógica antes do JavaScript


📹 Curso JavaScript - Guanabara

URL: https://www.youtube.com/playlist?list=PLntvgXM11X6pi7mW0O4ZmfUI1xDSIbmTm
Duração: ~60 aulas
Nível: Iniciante/Intermediário
Descrição: JavaScript completo do zero


📹 Map, Filter e Reduce - Rocketseat

URL: https://www.youtube.com/watch?v=JbzcLKiTThk
Duração: 15 min
Nível: Intermediário
Descrição: Os 3 métodos mais usados em arrays


📹 JavaScript Moderno (ES6+) - Willian Justen ✨ NOVO

URL: https://www.youtube.com/watch?v=VHRdSnJbNLg
Duração: 30 min
Nível: Intermediário
Descrição: Arrow functions, destructuring, spread operator


📹 Async/Await Descomplicado - Rocketseat ✨ NOVO

URL: https://www.youtube.com/watch?v=7Bs4-rqbCQc
Duração: 12 min
Nível: Intermediário
Descrição: Programação assíncrona simplificada


📹 Manipulação de DOM - Curso em Vídeo ✨ NOVO

URL: https://www.youtube.com/watch?v=WWZX8RWLxIk
Duração: 25 min
Nível: Iniciante
Descrição: Como interagir com elementos HTML via JS



Recursos Escritos

📄 MDN JavaScript Guide

URL: https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Guide
Tipo: Documentação
Descrição: Referência oficial mais completa


📄 JavaScript.info

URL: https://javascript.info/
Tipo: Tutorial Interativo
Descrição: Tutorial moderno e completo (traduzir para PT)



Jogos/Desafios

🎮 JavaScript Quiz - W3Schools ✨ NOVO

URL: https://www.w3schools.com/js/js_quiz.asp
Tipo: Quiz Interativo
Descrição: Teste seus conhecimentos


🎮 Exercism - JavaScript Track

URL: https://exercism.org/tracks/javascript
Tipo: Exercícios Práticos
Descrição: +100 exercícios com mentoria
🚀 GUIA COMPLETO - FASE 2: A LÓGICA (JavaScript)
📊 VISÃO GERAL
Antes (Atual)

✅ 5 exercícios básicos
✅ 3 vídeos
✅ 1 desafio final (Calc IMC)

Depois (Melhorado) ✨

✨ 10 exercícios progressivos (+ 5 novos)
✨ 6 vídeos educacionais (+ 3 novos)
✨ 1 jogo interativo (JavaScript Quiz)
✨ 1 mini-projeto (Calculadora Completa)
✨ 1 desafio final aprimorado (Calc IMC Avançada)
✨ Sistema de progresso visual
✨ Badges de conquistas


📚 TÓPICOS COMPLETOS DA FASE 2
🎯 Checklist de Conteúdo
1. Fundamentos JavaScript

✅ Variáveis (let, const, var)
✅ Tipos de dados (string, number, boolean, array, object)
✅ Operadores (aritméticos, lógicos, comparação)
✅ Template strings (${})

2. Lógica de Programação

✅ Condicionais (if, else, else if)
✅ Switch case
✅ Operador ternário (? :)
✅ Loops (for, while, forEach)

3. DOM Manipulation

✅ Seleção de elementos (querySelector, getElementById)
✅ Manipulação de conteúdo (textContent, innerHTML)
✅ Manipulação de estilos (classList, style)
✅ Criação de elementos (createElement, appendChild)

4. Eventos

✅ Event Listeners (click, input, submit)
✅ Event object (e.target, e.preventDefault)
✅ Eventos de teclado (keypress, keydown)
✅ Eventos de mouse (hover, mouseover)

5. ES6+ Moderno

✅ Arrow Functions (=>)
✅ Destructuring ({ }, [ ])
✅ Spread operator (...)
✅ Array Methods (map, filter, reduce, find)

6. Objetos e Arrays

✅ Criação e manipulação de objetos
✅ Métodos de arrays
✅ JSON (parse, stringify)
✅ Array de objetos


💪 EXERCÍCIOS PRÁTICOS (10 TOTAL)
✅ Exercícios Existentes (5)
🎯 Exercício 1: Variáveis e Matemática
Objetivo: Usar variáveis e operações matemáticas
Código Inicial:
javascriptfunction CalculoPreco() {
  // Defina as variáveis aqui
  const preco = 0;
  const quantidade = 0;
  const total = 0;

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>🛒 Carrinho de Compras</h3>
      <p>Preço unitário: R$ {preco}</p>
      <p>Quantidade: {quantidade}</p>
      <hr />
      <h2>Total: R$ {total}</h2>
    </div>
  );
}
Instrução:
Calcule o total multiplicando preço × quantidade. Use preco = 50 e quantidade = 3.
Dica: total = preco * quantidade

🎯 Exercício 2: Condicional (If/Else)
Objetivo: Mostrar mensagens diferentes baseado em condição
Código Inicial:
javascriptfunction StatusUsuario() {
  const isOnline = true; // Mude para false para testar

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>Status do Usuário</h3>
      {/* Use if/else aqui */}
      <p>Status: ?</p>
    </div>
  );
}
Instrução:
Mostre "🟢 Online" em verde se isOnline for true, ou "🔴 Offline" em vermelho se for false.
Dica: Use operador ternário: {isOnline ? "Online" : "Offline"}

🎯 Exercício 3: Arrays e .map()
Objetivo: Transformar array em lista HTML
Código Inicial:
javascriptfunction ListaDeTarefas() {
  const tarefas = ['Estudar React', 'Fazer exercícios', 'Praticar JavaScript'];

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>📝 Minhas Tarefas</h3>
      <ul>
        {/* Use .map() aqui para criar <li> */}
      </ul>
    </div>
  );
}
Instrução:
Use o método .map() para transformar cada item do array em um elemento <li>.
Dica: {tarefas.map((tarefa, index) => <li key={index}>{tarefa}</li>)}

🎯 Exercício 4: Objetos
Objetivo: Criar e exibir dados de um objeto
Código Inicial:
javascriptfunction PerfilUsuario() {
  const usuario = {
    nome: '',
    idade: 0,
    cidade: '',
    profissao: ''
  };

  return (
    <div style={{ 
      padding: '20px', 
      fontFamily: 'sans-serif',
      border: '2px solid #e5e7eb',
      borderRadius: '8px',
      maxWidth: '300px'
    }}>
      <h3>👤 Perfil</h3>
      {/* Exiba os dados do usuário aqui */}
    </div>
  );
}
Instrução:
Preencha o objeto usuário com seus dados e exiba-os usando {usuario.nome}, etc.
Dica: Acesse propriedades com ponto: usuario.nome, usuario.idade

🎯 Exercício 5: Eventos (onClick)
Objetivo: Criar interatividade com eventos
Código Inicial:
javascriptfunction BotaoInterativo() {
  const handleClick = () => {
    // Adicione um alert aqui
  };

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <button 
        style={{
          padding: '12px 24px',
          backgroundColor: '#3b82f6',
          color: 'white',
          border: 'none',
          borderRadius: '6px',
          cursor: 'pointer'
        }}
      >
        Clique em mim!
      </button>
    </div>
  );
}
Instrução:
Adicione um alert("Você clicou!") dentro de handleClick e conecte a função ao botão.
Dica: Use onClick={handleClick} no botão

✨ NOVOS EXERCÍCIOS (5 adicionais)
🎯 Exercício 6: Filtrar Array
Objetivo: Buscar nomes em uma lista
Código Inicial:
javascriptfunction BuscaNomes() {
  const [busca, setBusca] = React.useState('');
  
  const nomes = [
    'Ana Silva',
    'Bruno Costa',
    'Carlos Santos',
    'Diana Oliveira',
    'Eduardo Lima',
    'Fernanda Souza'
  ];

  // Filtre os nomes aqui
  const nomesFiltrados = nomes;

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>🔍 Buscar Pessoa</h3>
      <input
        type="text"
        placeholder="Digite um nome..."
        value={busca}
        onChange={(e) => setBusca(e.target.value)}
        style={{
          padding: '10px',
          width: '100%',
          marginBottom: '20px',
          border: '1px solid #d1d5db',
          borderRadius: '6px'
        }}
      />
      <div>
        {nomesFiltrados.length === 0 ? (
          <p style={{ color: '#6b7280' }}>Nenhum resultado encontrado</p>
        ) : (
          <ul style={{ listStyle: 'none', padding: 0 }}>
            {nomesFiltrados.map((nome, i) => (
              <li 
                key={i}
                style={{
                  padding: '10px',
                  backgroundColor: '#f3f4f6',
                  marginBottom: '8px',
                  borderRadius: '6px'
                }}
              >
                {nome}
              </li>
            ))}
          </ul>
        )}
      </div>
    </div>
  );
}
Instrução:
Use .filter() para mostrar apenas nomes que contenham o texto digitado na busca (toLowerCase para ignorar maiúsculas).
Dica: nomes.filter(nome => nome.toLowerCase().includes(busca.toLowerCase()))
Dificuldade: ⭐⭐ Médio | Tempo: 15 min

🎯 Exercício 7: Timer/Cronômetro
Objetivo: Criar um cronômetro simples
Código Inicial:
javascriptfunction Cronometro() {
  const [segundos, setSegundos] = React.useState(0);
  const [ativo, setAtivo] = React.useState(false);

  React.useEffect(() => {
    let intervalo = null;
    
    if (ativo) {
      intervalo = setInterval(() => {
        setSegundos(s => s + 1);
      }, 1000);
    } else {
      clearInterval(intervalo);
    }
    
    return () => clearInterval(intervalo);
  }, [ativo]);

  const toggleTimer = () => {
    setAtivo(!ativo);
  };

  const resetTimer = () => {
    setSegundos(0);
    setAtivo(false);
  };

  const formatarTempo = (seg) => {
    const mins = Math.floor(seg / 60);
    const secs = seg % 60;
    return `${mins.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`;
  };

  return (
    <div style={{ 
      padding: '40px', 
      fontFamily: 'sans-serif',
      textAlign: 'center'
    }}>
      <h3>⏱️ Cronômetro</h3>
      <div style={{
        fontSize: '48px',
        fontWeight: 'bold',
        margin: '20px 0',
        color: '#1f2937'
      }}>
        {formatarTempo(segundos)}
      </div>
      <div style={{ display: 'flex', gap: '10px', justifyContent: 'center' }}>
        <button 
          onClick={toggleTimer}
          style={{
            padding: '12px 24px',
            backgroundColor: ativo ? '#ef4444' : '#10b981',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            cursor: 'pointer',
            fontWeight: 'bold'
          }}
        >
          {ativo ? '⏸️ Pausar' : '▶️ Iniciar'}
        </button>
        <button 
          onClick={resetTimer}
          style={{
            padding: '12px 24px',
            backgroundColor: '#6b7280',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            cursor: 'pointer'
          }}
        >
          🔄 Resetar
        </button>
      </div>
    </div>
  );
}
Instrução:
O cronômetro já está funcional! Personalize:

Mude as cores dos botões
Adicione um botão de "Lap" (marcar tempo)
Adicione um som quando chegar em 1 minuto

Dica: Para lap, crie um array de tempos marcados com useState
Dificuldade: ⭐⭐ Médio | Tempo: 20 min

🎯 Exercício 8: Gerador de Cor Aleatória
Objetivo: Gerar cores hexadecimais aleatórias
Código Inicial:
javascriptfunction GeradorDeCores() {
  const [cor, setCor] = React.useState('#3b82f6');

  const gerarCorAleatoria = () => {
    // Crie uma cor hex aleatória aqui
    const novaCor = '#000000';
    setCor(novaCor);
  };

  return (
    <div style={{ 
      padding: '40px', 
      fontFamily: 'sans-serif',
      textAlign: 'center'
    }}>
      <div
        style={{
          width: '200px',
          height: '200px',
          backgroundColor: cor,
          margin: '0 auto 20px',
          borderRadius: '12px',
          boxShadow: '0 4px 6px rgba(0,0,0,0.1)',
          transition: 'all 0.3s ease'
        }}
      />
      <h3 style={{ 
        fontFamily: 'monospace',
        fontSize: '24px',
        color: '#1f2937'
      }}>
        {cor}
      </h3>
      <button
        onClick={gerarCorAleatoria}
        style={{
          padding: '12px 24px',
          backgroundColor: '#1f2937',
          color: 'white',
          border: 'none',
          borderRadius: '6px',
          cursor: 'pointer',
          marginTop: '20px',
          fontWeight: 'bold'
        }}
      >
        🎨 Gerar Nova Cor
      </button>
    </div>
  );
}
Instrução:
Gere uma cor hexadecimal aleatória (#RRGGBB). Use Math.random() para gerar valores de 0-255 e converta para hex.
Dica:
javascriptconst r = Math.floor(Math.random() * 256).toString(16).padStart(2, '0');
const g = Math.floor(Math.random() * 256).toString(16).padStart(2, '0');
const b = Math.floor(Math.random() * 256).toString(16).padStart(2, '0');
const novaCor = `#${r}${g}${b}`;
Dificuldade: ⭐⭐ Médio | Tempo: 15 min

🎯 Exercício 9: Validação de Formulário
Objetivo: Validar campos antes de enviar
Código Inicial:
javascriptfunction FormularioValidado() {
  const [nome, setNome] = React.useState('');
  const [email, setEmail] = React.useState('');
  const [senha, setSenha] = React.useState('');
  const [erros, setErros] = React.useState({});

  const validarFormulario = () => {
    const novosErros = {};

    // Adicione validações aqui
    if (nome.trim() === '') {
      novosErros.nome = 'Nome é obrigatório';
    }

    // TODO: Validar email e senha

    setErros(novosErros);
    return Object.keys(novosErros).length === 0;
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    
    if (validarFormulario()) {
      alert('✅ Formulário válido! Dados: ' + JSON.stringify({ nome, email, senha }));
    }
  };

  return (
    <div style={{ 
      padding: '20px', 
      maxWidth: '400px',
      fontFamily: 'sans-serif'
    }}>
      <h3>📝 Cadastro</h3>
      <form onSubmit={handleSubmit}>
        <div style={{ marginBottom: '15px' }}>
          <label style={{ display: 'block', marginBottom: '5px', fontWeight: 'bold' }}>
            Nome:
          </label>
          <input
            type="text"
            value={nome}
            onChange={(e) => setNome(e.target.value)}
            style={{
              width: '100%',
              padding: '10px',
              border: erros.nome ? '2px solid #ef4444' : '1px solid #d1d5db',
              borderRadius: '6px'
            }}
          />
          {erros.nome && (
            <span style={{ color: '#ef4444', fontSize: '12px' }}>{erros.nome}</span>
          )}
        </div>

        <div style={{ marginBottom: '15px' }}>
          <label style={{ display: 'block', marginBottom: '5px', fontWeight: 'bold' }}>
            Email:
          </label>
          <input
            type="email"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            style={{
              width: '100%',
              padding: '10px',
              border: erros.email ? '2px solid #ef4444' : '1px solid #d1d5db',
              borderRadius: '6px'
            }}
          />
          {erros.email && (
            <span style={{ color: '#ef4444', fontSize: '12px' }}>{erros.email}</span>
          )}
        </div>

        <div style={{ marginBottom: '15px' }}>
          <label style={{ display: 'block', marginBottom: '5px', fontWeight: 'bold' }}>
            Senha:
          </label>
          <input
            type="password"
            value={senha}
            onChange={(e) => setSenha(e.target.value)}
            style={{
              width: '100%',
              padding: '10px',
              border: erros.senha ? '2px solid #ef4444' : '1px solid #d1d5db',
              borderRadius: '6px'
            }}
          />
          {erros.senha && (
            <span style={{ color: '#ef4444', fontSize: '12px' }}>{erros.senha}</span>
          )}
        </div>

        <button
          type="submit"
          style={{
            width: '100%',
            padding: '12px',
            backgroundColor: '#3b82f6',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            cursor: 'pointer',
            fontWeight: 'bold'
          }}
        >
          Cadastrar
        </button>
      </form>
    </div>
  );
}
Instrução:
Complete as validações:

Nome: mínimo 3 caracteres
Email: deve conter @ e .
Senha: mínimo 6 caracteres

Dica:

Email: email.includes('@') && email.includes('.')
Senha: senha.length >= 6

Dificuldade: ⭐⭐⭐ Difícil | Tempo: 25 min

🎯 Exercício 10: Accordion/FAQ
Objetivo: Criar seções que abrem/fecham
Código Inicial:
javascriptfunction Accordion() {
  const [abaAberta, setAbaAberta] = React.useState(null);

  const perguntas = [
    {
      id: 1,
      pergunta: 'O que é JavaScript?',
      resposta: 'JavaScript é uma linguagem de programação que torna as páginas web interativas.'
    },
    {
      id: 2,
      pergunta: 'O que é React?',
      resposta: 'React é uma biblioteca JavaScript para construir interfaces de usuário.'
    },
    {
      id: 3,
      pergunta: 'O que é uma variável?',
      resposta: 'Uma variável é um espaço na memória para armazenar dados.'
    }
  ];

  const toggleAba = (id) => {
    // Se a aba já está aberta, fecha. Senão, abre.
    setAbaAberta(abaAberta === id ? null : id);
  };

  return (
    <div style={{ 
      padding: '20px', 
      maxWidth: '600px',
      fontFamily: 'sans-serif'
    }}>
      <h3>❓ Perguntas Frequentes</h3>
      {perguntas.map((item) => (
        <div 
          key={item.id}
          style={{
            marginBottom: '10px',
            border: '1px solid #e5e7eb',
            borderRadius: '8px',
            overflow: 'hidden'
          }}
        >
          <button
            onClick={() => toggleAba(item.id)}
            style={{
              width: '100%',
              padding: '15px',
              backgroundColor: abaAberta === item.id ? '#3b82f6' : '#f3f4f6',
              color: abaAberta === item.id ? 'white' : '#1f2937',
              border: 'none',
              textAlign: 'left',
              cursor: 'pointer',
              fontWeight: 'bold',
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center'
            }}
          >
            {item.pergunta}
            <span>{abaAberta === item.id ? '▲' : '▼'}</span>
          </button>
          {abaAberta === item.id && (
            <div style={{
              padding: '15px',
              backgroundColor: 'white',
              borderTop: '1px solid #e5e7eb'
            }}>
              {item.resposta}
            </div>
          )}
        </div>
      ))}
    </div>
  );
}
Instrução:
O accordion já funciona! Melhore:

Adicione animação de entrada (transition)
Mude o ícone para + e - ao invés de setas
Adicione mais perguntas ao array

Dica: Para animação suave, use CSS transition: 'all 0.3s ease'
Dificuldade: ⭐⭐ Médio | Tempo: 20 min

🎥 RECURSOS EDUCACIONAIS
Vídeos Recomendados (6 total)

📹 Curso Lógica de Programação - Guanabara

URL: https://www.youtube.com/playlist?list=PLHz_AreHm4dlsK3Nr9GVvXCbpQyHQl1o1
Duração: ~40 aulas
Nível: Iniciante
Descrição: Fundamentos de lógica antes do JavaScript


📹 Curso JavaScript - Guanabara

URL: https://www.youtube.com/playlist?list=PLntvgXM11X6pi7mW0O4ZmfUI1xDSIbmTm
Duração: ~60 aulas
Nível: Iniciante/Intermediário
Descrição: JavaScript completo do zero


📹 Map, Filter e Reduce - Rocketseat

URL: https://www.youtube.com/watch?v=JbzcLKiTThk
Duração: 15 min
Nível: Intermediário
Descrição: Os 3 métodos mais usados em arrays


📹 JavaScript Moderno (ES6+) - Willian Justen ✨ NOVO

URL: https://www.youtube.com/watch?v=VHRdSnJbNLg
Duração: 30 min
Nível: Intermediário
Descrição: Arrow functions, destructuring, spread operator


📹 Async/Await Descomplicado - Rocketseat ✨ NOVO

URL: https://www.youtube.com/watch?v=7Bs4-rqbCQc
Duração: 12 min
Nível: Intermediário
Descrição: Programação assíncrona simplificada


📹 Manipulação de DOM - Curso em Vídeo ✨ NOVO

URL: https://www.youtube.com/watch?v=WWZX8RWLxIk
Duração: 25 min
Nível: Iniciante
Descrição: Como interagir com elementos HTML via JS



Recursos Escritos

📄 MDN JavaScript Guide

URL: https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Guide
Tipo: Documentação
Descrição: Referência oficial mais completa


📄 JavaScript.info

URL: https://javascript.info/
Tipo: Tutorial Interativo
Descrição: Tutorial moderno e completo (traduzir para PT)



Jogos/Desafios

🎮 JavaScript Quiz - W3Schools ✨ NOVO

URL: https://www.w3schools.com/js/js_quiz.asp
Tipo: Quiz Interativo
Descrição: Teste seus conhecimentos


🎮 Exercism - JavaScript Track

URL: https://exercism.org/tracks/javascript
Tipo: Exercícios Práticos
Descrição: +100 exercícios com mentoria




🎨 MINI-PROJETO: CALCULADORA COMPLETA
Objetivo
Criar uma calculadora funcional com todas as operações básicas e histórico.
Requisitos Obrigatórios

Interface:

Display para mostrar números e resultado
Botões 0-9
Botões de operações (+, -, ×, ÷)
Botão = (igual)
Botão C (limpar)
Botão CE (limpar entrada)


Funcionalidades:

Operações básicas funcionais
Operações em cadeia (2 + 3 + 4 = 9)
Números decimais (ponto)
Tratamento de divisão por zero


Bônus (+pontos):

Histórico de cálculos
Teclas do teclado funcionam
Modo dark/light
Animações nos botões



Estrutura Base
javascriptfunction Calculadora() {
  const [display, setDisplay] = React.useState('0');
  const [valorAnterior, setValorAnterior] = React.useState(null);
  const [operacao, setOperacao] = React.useState(null);
  const [novoNumero, setNovoNumero] = React.useState(true);

  const adicionarNumero = (num) => {
    // Implementar lógica
  };

  const adicionarOperacao = (op) => {
    // Implementar lógica
  };

  const calcular = () => {
    // Implementar lógica
  };

  const limpar = () => {
    setDisplay('0');
    setValorAnterior(null);
    setOperacao(null);
    setNovoNumero(true);
  };

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      {/* Construa a calculadora aqui */}
    </div>
  );
}
Critérios de Avaliação

✅ Código limpo e organizado (30%)
✅ Todas as operações funcionam (40%)
✅ Interface bonita e responsiva (20%)
✅ Bônus implementados (10%)

Prazo Sugerido
1 semana para completar

🏆 DESAFIO FINAL: CALCULADORA IMC AVANÇADA
Upgrade do Desafio Original
Versão Básica (Original)

Input peso e altura
Botão calcular
Mostrar resultado do IMC

Versão Avançada (Nova) ✨
Requisitos Extras:

Interface Profissional:

Design moderno com gradientes
Inputs estilizados com ícones
Animações suaves


Validações:

Peso: 20kg - 300kg
Altura: 0.5m - 2.5m
Mensagens de erro claras


Feedback Visual Completo:

Barra de progresso colorida
Tabela de classificação
Emoji baseado no resultado
Dicas personalizadas


Funcionalidades Extra:

Histórico de medições
Gráfico de evolução (opcional)
Calcular peso ideal
Compartilhar resultado


Responsividade:

Mobile first
Funciona em todos os tamanhos



Tabela de Classificação IMC
IMCClassificaçãoCorEmoji< 18.5Abaixo do peso🔵 Azul😟18.5 - 24.9Peso normal🟢 Verde😊25 - 29.9Sobrepeso🟡 Amarelo😐30 - 34.9Obesidade I🟠 Laranja😰35 - 39.9Obesidade II🔴 Vermelho😨≥ 40Obesidade III🔴 Vermelho Escuro😱
Template Inicial
javascriptfunction CalculadoraIMC() {
  const [peso, setPeso] = React.useState('');
  const [altura, setAltura] = React.useState('');
  const [resultado, setResultado] = React.useState(null);
  const [historico, setHistorico] = React.useState([]);

  const calcularIMC = () => {
    const p = parseFloat(peso);
    const a = parseFloat(altura);
    
    if (isNaN(p) || isNaN(a) || p <= 0 || a <= 0) {
      alert('Por favor, insira valores válidos!');
      return;
    }
    
    const imc = (p / (a * a)).toFixed(1);
    const novoResultado = {
      imc,
      classificacao: obterClassificacao(imc),
      data: new Date().toLocaleDateString()
    };
    
    setResultado(novoResultado);
    setHistorico([novoResultado, ...historico]);
  };

  const obterClassificacao = (imc) => {
    if (imc < 18.5) return { texto: 'Abaixo do peso', cor: '#3b82f6', emoji: '😟' };
    if (imc < 25) return { texto: 'Peso normal', cor: '#10b981', emoji: '😊' };
    if (imc < 30) return { texto: 'Sobrepeso', cor: '#f59e0b', emoji: '😐' };
    if (imc < 35) return { texto: 'Obesidade I', cor: '#f97316', emoji: '😰' };
    if (imc < 40) return { texto: 'Obesidade II', cor: '#ef4444', emoji: '😨' };
    return { texto: 'Obesidade III', cor: '#991b1b', emoji: '😱' };
  };

  return (
    <div style={{ /* Seu design aqui */ }}>
      {/* Implemente a calculadora completa */}
    </div>
  );
}
Checklist de Implementação

 Inputs com validação
 Cálculo correto do IMC
 Feedback visual colorido
 Tabela de classificação
 Histórico funcionando
 Design responsivo
 Animações suaves
 Bônus: Gráfico de evolução


🎯 SISTEMA DE PROGRESSO VISUAL
Indicadores por Exercício
📊 Fase 2: JavaScript
━━━━━━━━━━━━━━━━━━━━ 80%

✅ Ex 1: Variáveis (5 min) ⭐
✅ Ex 2: Condicional (7 min) ⭐
✅ Ex 3: Arrays .map() (10 min) ⭐
✅ Ex 4: Objetos (10 min) ⭐
✅ Ex 5: Eventos (8 min) ⭐
✅ Ex 6: Filtrar Array (15 min) ⭐⭐
✅ Ex 7: Cronômetro (20 min) ⭐⭐
✅ Ex 8: Gerador Cores (15 min) ⭐⭐
⏳ Ex 9: Validação Form (25 min) ⭐⭐⭐ - 60% completo
🔒 Ex 10: Accordion (20 min) ⭐⭐

📦 Mini-Projeto: Calculadora
🏆 Desafio Final: IMC Avançada
Métricas de Desempenho
🎯 Sua Jornada JavaScript

Exercícios Completos: 8/10 (80%)
Tempo Estudado: 12h 30min
Sequência: 🔥 7 dias seguidos
Próximo Objetivo: 🏅 Badge "Lógico"

📈 Progresso por Tópico:
- Variáveis & Operadores: ████████████ 100%
- Condicionais & Loops: ██████████░░ 85%
- Arrays & Objetos: ████████░░░░ 70%
- DOM & Eventos: ██████░░░░░░ 55%
- ES6+ Moderno: ████░░░░░░░░ 40%

🏅 BADGES/CONQUISTAS DA FASE 2
1. ✨ Primeiro Script

Condição: Completar exercício 1
Desbloqueado por: 98% dos alunos
Recompensa: +10 XP

2. 🧠 Pensador Lógico

Condição: Completar 5 exercícios
Desbloqueado por: 65% dos alunos
Recompensa: +50 XP

3. 🎮 Mestre dos Arrays

Condição: Completar exercícios 3 e 6
Desbloqueado por: 45% dos alunos
Recompensa: +30 XP

4. ⚡ Programador Ágil

Condição: Completar mini-projeto em menos de 1 semana
Desbloqueado por: 35% dos alunos
Recompensa: +100 XP

5. 🏆 Lógico Mestre

Condição: Completar todos os 10 exercícios + mini-projeto + desafio
Desbloqueado por: 20% dos alunos
Recompensa: +200 XP + Badge especial

6. 🚀 Velocista

Condição: Completar 3 exercícios em 1 dia
Desbloqueado por: 25% dos alunos
Recompensa: +75 XP


📱 MELHORIAS NA INTERFACE
1. Painel de Exercícios Aprimorado
┌─────────────────────────────────┐
│ 💻 Fase 2: JavaScript           │
│ ━━━━━━━━━━━━━━━━━━━━ 80%       │
├─────────────────────────────────┤
│                                 │
│ 📋 EXERCÍCIOS (8/10)            │
│                                 │
│ ✅ 1. Variáveis      ⭐ 5min    │
│ ✅ 2. Condicional    ⭐ 7min    │
│ ✅ 3. Arrays .map()  ⭐ 10min   │
│ ✅ 4. Objetos        ⭐ 10min   │
│ ✅ 5. Eventos        ⭐ 8min    │
│ ✅ 6. Filtrar Array  ⭐⭐ 15min │
│ ✅ 7. Cronômetro     ⭐⭐ 20min │
│ ✅ 8. Cores          ⭐⭐ 15min │
│ ⏳ 9. Validação      ⭐⭐⭐ 25min│
│ 🔒 10. Accordion     ⭐⭐ 20min │
│                                 │
│ 📦 Mini-Projeto: Calculadora    │
│ 🏆 Desafio: IMC Avançada        │
│                                 │
│ [📚 Recursos] [🎥 Vídeos]       │
└─────────────────────────────────┘
2. Recursos Interativos
Vídeos com Preview:
🎥 Curso JavaScript - Guanabara
   └─ 60 aulas | 20h total
   └─ ⭐⭐⭐⭐⭐ 4.9/5 (12k avaliações)
   └─ [▶️ Assistir] [✓ Marcar como visto]

🎥 Map, Filter, Reduce - Rocketseat
   └─ 15 min | Intermediário
   └─ ✅ Você assistiu (3 dias atrás)
   └─ [🔄 Assistir novamente]
3. Console de Código Integrado
┌─────────────────────────────────┐
│ 📝 Editor                       │
├─────────────────────────────────┤
│ 1  function calcular() {        │
│ 2    const total = 10 + 5;     │
│ 3    console.log(total);       │
│ 4  }                            │
│ 5                               │
├─────────────────────────────────┤
│ 🖥️ Console                      │
├─────────────────────────────────┤
│ > 15                            │
│                                 │
│ [🧹 Limpar] [▶️ Executar]       │
└─────────────────────────────────┘

📊 COMPARAÇÃO: ANTES vs DEPOIS
MétricaAntesDepoisCrescimentoExercícios510+100% 🚀Vídeos36+100% 🚀Jogos/Quiz02∞ ✨Mini-Projetos01∞ ✨Badges06∞ ✨Tempo Estimado4-6 sem5-7 sem+50% conteúdoTópicos26+200% 🚀Recursos Escritos02∞ ✨
Resumo Visual
FASE 2: ANTES
├── 5 exercícios básicos
├── 3 vídeos
└── 1 desafio

FASE 2: DEPOIS ✨
├── 10 exercícios (progressão clara)
├── 6 vídeos (cobertura completa)
├── 2 jogos/quizzes interativos
├── 1 mini-projeto (calculadora)
├── 1 desafio avançado (IMC++)
├── 6 badges de conquista
├── Sistema de progresso visual
└── 2 recursos escritos (MDN + JavaScript.info)

AUMENTO TOTAL DE CONTEÚDO: +250% 🚀🚀🚀

✅ CHECKLIST DE IMPLEMENTAÇÃO
Prioridade 1 - Implementar Agora

 Adicionar 5 novos exercícios ao exercises.js
 Atualizar courseData.jsx com novos recursos
 Atualizar tópicos completos da Fase 2
 Adicionar links dos 6 vídeos

Prioridade 2 - Segunda Etapa

 Implementar sistema de badges
 Criar template do mini-projeto (Calculadora)
 Criar template do desafio final (IMC Avançada)
 Adicionar indicadores de dificuldade (⭐⭐⭐)

Prioridade 3 - Refinamento

 Adicionar tempo estimado por exercício
 Implementar console interativo
 Adicionar preview de vídeos
 Sistema de "Marcar como concluído"


🚀 PRÓXIMOS PASSOS

✅ Revisar e aprovar este guia
✅ Implementar os 5 novos exercícios
✅ Atualizar dados do curso (courseData.jsx)
✅ Testar todos os exercícios
✅ Criar templates dos projetos
✅ Adicionar sistema de progresso
✅ Deploy e monitoramento


🎯 METAS DE APRENDIZADO
Ao completar a Fase 2, o aluno será capaz de:
✅ Escrever lógica JavaScript do zero
✅ Manipular o DOM com confiança
✅ Trabalhar com arrays e objetos
✅ Criar interatividade com eventos
✅ Usar métodos modernos ES6+ (map, filter, arrow functions)
✅ Validar formulários
✅ Construir aplicações interativas completas
✅ Entender programação assíncrona básica
Próxima Fase: React Básico (componentes, JSX, state)

Pronto para implementar a Fase 2 melhorada? 🚀💻
// EXERCÍCIOS DA FASE 2: JAVASCRIPT (10 EXERCÍCIOS COMPLETOS)
// Adicionar ao arquivo src/data/exercises.js

// ============================================
// FASE 2: JAVASCRIPT (10 EXERCÍCIOS)
// ============================================

{
  id: 'ex4',
  moduleId: 'fase2',
  title: "Variáveis e Matemática",
  difficulty: 1,
  estimatedTime: "5 min",
  instruction: "Calcule o total multiplicando preço × quantidade. Use preco = 50 e quantidade = 3.",
  hint: "Multiplique: total = preco * quantidade",
  initialCode: `function CalculoPreco() {
  // Defina as variáveis aqui
  const preco = 0;
  const quantidade = 0;
  const total = 0;

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>🛒 Carrinho de Compras</h3>
      <p>Preço unitário: R$ {preco}</p>
      <p>Quantidade: {quantidade}</p>
      <hr />
      <h2>Total: R$ {total}</h2>
    </div>
  );
}`,
  tags: ['variáveis', 'operadores', 'matemática']
},

{
  id: 'ex5',
  moduleId: 'fase2',
  title: "Condicional (If/Else)",
  difficulty: 1,
  estimatedTime: "7 min",
  instruction: "Mostre '🟢 Online' em verde se isOnline for true, ou '🔴 Offline' em vermelho se for false.",
  hint: "Use operador ternário: {isOnline ? 'Online' : 'Offline'}",
  initialCode: `function StatusUsuario() {
  const isOnline = true; // Mude para false para testar

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>Status do Usuário</h3>
      {/* Use if/else ou operador ternário aqui */}
      <p style={{ color: '???' }}>
        Status: ???
      </p>
    </div>
  );
}`,
  tags: ['condicional', 'if-else', 'operador-ternário']
},

{
  id: 'ex6',
  moduleId: 'fase2',
  title: "Arrays e .map()",
  difficulty: 1,
  estimatedTime: "10 min",
  instruction: "Use o método .map() para transformar cada item do array em um elemento <li>.",
  hint: "{tarefas.map((tarefa, index) => <li key={index}>{tarefa}</li>)}",
  initialCode: `function ListaDeTarefas() {
  const tarefas = ['Estudar React', 'Fazer exercícios', 'Praticar JavaScript'];

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>📝 Minhas Tarefas</h3>
      <ul>
        {/* Use .map() aqui para criar <li> */}
      </ul>
    </div>
  );
}`,
  tags: ['arrays', 'map', 'iteração']
},

{
  id: 'ex7',
  moduleId: 'fase2',
  title: "Objetos",
  difficulty: 1,
  estimatedTime: "10 min",
  instruction: "Preencha o objeto usuário com seus dados e exiba-os usando {usuario.nome}, etc.",
  hint: "Acesse propriedades com ponto: usuario.nome, usuario.idade",
  initialCode: `function PerfilUsuario() {
  const usuario = {
    nome: '',
    idade: 0,
    cidade: '',
    profissao: ''
  };

  return (
    <div style={{ 
      padding: '20px', 
      fontFamily: 'sans-serif',
      border: '2px solid #e5e7eb',
      borderRadius: '8px',
      maxWidth: '300px'
    }}>
      <h3>👤 Perfil</h3>
      {/* Exiba os dados do usuário aqui */}
      <p><strong>Nome:</strong> ???</p>
      <p><strong>Idade:</strong> ???</p>
      <p><strong>Cidade:</strong> ???</p>
      <p><strong>Profissão:</strong> ???</p>
    </div>
  );
}`,
  tags: ['objetos', 'propriedades', 'dados']
},

{
  id: 'ex8',
  moduleId: 'fase2',
  title: "Eventos (onClick)",
  difficulty: 1,
  estimatedTime: "8 min",
  instruction: "Adicione um alert('Você clicou!') dentro de handleClick e conecte a função ao botão.",
  hint: "Use onClick={handleClick} no botão",
  initialCode: `function BotaoInterativo() {
  const handleClick = () => {
    // Adicione um alert aqui
  };

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <button 
        style={{
          padding: '12px 24px',
          backgroundColor: '#3b82f6',
          color: 'white',
          border: 'none',
          borderRadius: '6px',
          cursor: 'pointer'
        }}
      >
        Clique em mim!
      </button>
    </div>
  );
}`,
  tags: ['eventos', 'onClick', 'interatividade']
},

{
  id: 'ex9',
  moduleId: 'fase2',
  title: "Filtrar Array",
  difficulty: 2,
  estimatedTime: "15 min",
  instruction: "Use .filter() para mostrar apenas nomes que contenham o texto digitado na busca (toLowerCase para ignorar maiúsculas).",
  hint: "nomes.filter(nome => nome.toLowerCase().includes(busca.toLowerCase()))",
  initialCode: `function BuscaNomes() {
  const [busca, setBusca] = React.useState('');
  
  const nomes = [
    'Ana Silva',
    'Bruno Costa',
    'Carlos Santos',
    'Diana Oliveira',
    'Eduardo Lima',
    'Fernanda Souza'
  ];

  // Filtre os nomes aqui
  const nomesFiltrados = nomes;

  return (
    <div style={{ padding: '20px', fontFamily: 'sans-serif' }}>
      <h3>🔍 Buscar Pessoa</h3>
      <input
        type="text"
        placeholder="Digite um nome..."
        value={busca}
        onChange={(e) => setBusca(e.target.value)}
        style={{
          padding: '10px',
          width: '100%',
          marginBottom: '20px',
          border: '1px solid #d1d5db',
          borderRadius: '6px'
        }}
      />
      <div>
        {nomesFiltrados.length === 0 ? (
          <p style={{ color: '#6b7280' }}>Nenhum resultado encontrado</p>
        ) : (
          <ul style={{ listStyle: 'none', padding: 0 }}>
            {nomesFiltrados.map((nome, i) => (
              <li 
                key={i}
                style={{
                  padding: '10px',
                  backgroundColor: '#f3f4f6',
                  marginBottom: '8px',
                  borderRadius: '6px'
                }}
              >
                {nome}
              </li>
            ))}
          </ul>
        )}
      </div>
    </div>
  );
}`,
  tags: ['filter', 'arrays', 'busca', 'intermediário']
},

{
  id: 'ex10',
  moduleId: 'fase2',
  title: "Timer/Cronômetro",
  difficulty: 2,
  estimatedTime: "20 min",
  instruction: "O cronômetro já está funcional! Personalize: mude as cores dos botões, adicione um botão de 'Lap' (marcar tempo), ou adicione um som quando chegar em 1 minuto.",
  hint: "Para lap, crie um array de tempos marcados com useState",
  initialCode: `function Cronometro() {
  const [segundos, setSegundos] = React.useState(0);
  const [ativo, setAtivo] = React.useState(false);

  React.useEffect(() => {
    let intervalo = null;
    
    if (ativo) {
      intervalo = setInterval(() => {
        setSegundos(s => s + 1);
      }, 1000);
    } else {
      clearInterval(intervalo);
    }
    
    return () => clearInterval(intervalo);
  }, [ativo]);

  const toggleTimer = () => {
    setAtivo(!ativo);
  };

  const resetTimer = () => {
    setSegundos(0);
    setAtivo(false);
  };

  const formatarTempo = (seg) => {
    const mins = Math.floor(seg / 60);
    const secs = seg % 60;
    return \`\${mins.toString().padStart(2, '0')}:\${secs.toString().padStart(2, '0')}\`;
  };

  return (
    <div style={{ 
      padding: '40px', 
      fontFamily: 'sans-serif',
      textAlign: 'center'
    }}>
      <h3>⏱️ Cronômetro</h3>
      <div style={{
        fontSize: '48px',
        fontWeight: 'bold',
        margin: '20px 0',
        color: '#1f2937'
      }}>
        {formatarTempo(segundos)}
      </div>
      <div style={{ display: 'flex', gap: '10px', justifyContent: 'center' }}>
        <button 
          onClick={toggleTimer}
          style={{
            padding: '12px 24px',
            backgroundColor: ativo ? '#ef4444' : '#10b981',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            cursor: 'pointer',
            fontWeight: 'bold'
          }}
        >
          {ativo ? '⏸️ Pausar' : '▶️ Iniciar'}
        </button>
        <button 
          onClick={resetTimer}
          style={{
            padding: '12px 24px',
            backgroundColor: '#6b7280',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            cursor: 'pointer'
          }}
        >
          🔄 Resetar
        </button>
      </div>
    </div>
  );
}`,
  tags: ['useEffect', 'timer', 'setInterval', 'intermediário']
},

{
  id: 'ex11',
  moduleId: 'fase2',
  title: "Gerador de Cor Aleatória",
  difficulty: 2,
  estimatedTime: "15 min",
  instruction: "Gere uma cor hexadecimal aleatória (#RRGGBB). Use Math.random() para gerar valores de 0-255 e converta para hex.",
  hint: "const r = Math.floor(Math.random() * 256).toString(16).padStart(2, '0');",
  initialCode: `function GeradorDeCores() {
  const [cor, setCor] = React.useState('#3b82f6');

  const gerarCorAleatoria = () => {
    // Crie uma cor hex aleatória aqui
    const novaCor = '#000000';
    setCor(novaCor);
  };

  return (
    <div style={{ 
      padding: '40px', 
      fontFamily: 'sans-serif',
      textAlign: 'center'
    }}>
      <div
        style={{
          width: '200px',
          height: '200px',
          backgroundColor: cor,
          margin: '0 auto 20px',
          borderRadius: '12px',
          boxShadow: '0 4px 6px rgba(0,0,0,0.1)',
          transition: 'all 0.3s ease'
        }}
      />
      <h3 style={{ 
        fontFamily: 'monospace',
        fontSize: '24px',
        color: '#1f2937'
      }}>
        {cor}
      </h3>
      <button
        onClick={gerarCorAleatoria}
        style={{
          padding: '12px 24px',
          backgroundColor: '#1f2937',
          color: 'white',
          border: 'none',
          borderRadius: '6px',
          cursor: 'pointer',
          marginTop: '20px',
          fontWeight: 'bold'
        }}
      >
        🎨 Gerar Nova Cor
      </button>
    </div>
  );
}`,
  tags: ['Math.random', 'hexadecimal', 'cores', 'intermediário']
},

{
  id: 'ex12',
  moduleId: 'fase2',
  title: "Validação de Formulário",
  difficulty: 3,
  estimatedTime: "25 min",
  instruction: "Complete as validações: Nome (mín 3 caracteres), Email (deve conter @ e .), Senha (mín 6 caracteres).",
  hint: "Email: email.includes('@') && email.includes('.') | Senha: senha.length >= 6",
  initialCode: `function FormularioValidado() {
  const [nome, setNome] = React.useState('');
  const [email, setEmail] = React.useState('');
  const [senha, setSenha] = React.useState('');
  const [erros, setErros] = React.useState({});

  const validarFormulario = () => {
    const novosErros = {};

    // Adicione validações aqui
    if (nome.trim() === '') {
      novosErros.nome = 'Nome é obrigatório';
    }

    // TODO: Validar email e senha

    setErros(novosErros);
    return Object.keys(novosErros).length === 0;
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    
    if (validarFormulario()) {
      alert('✅ Formulário válido! Dados: ' + JSON.stringify({ nome, email, senha }));
    }
  };

  return (
    <div style={{ 
      padding: '20px', 
      maxWidth: '400px',
      fontFamily: 'sans-serif'
    }}>
      <h3>📝 Cadastro</h3>
      <form onSubmit={handleSubmit}>
        <div style={{ marginBottom: '15px' }}>
          <label style={{ display: 'block', marginBottom: '5px', fontWeight: 'bold' }}>
            Nome:
          </label>
          <input
            type="text"
            value={nome}
            onChange={(e) => setNome(e.target.value)}
            style={{
              width: '100%',
              padding: '10px',
              border: erros.nome ? '2px solid #ef4444' : '1px solid #d1d5db',
              borderRadius: '6px'
            }}
          />
          {erros.nome && (
            <span style={{ color: '#ef4444', fontSize: '12px' }}>{erros.nome}</span>
          )}
        </div>

        <div style={{ marginBottom: '15px' }}>
          <label style={{ display: 'block', marginBottom: '5px', fontWeight: 'bold' }}>
            Email:
          </label>
          <input
            type="email"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            style={{
              width: '100%',
              padding: '10px',
              border: erros.email ? '2px solid #ef4444' : '1px solid #d1d5db',
              borderRadius: '6px'
            }}
          />
          {erros.email && (
            <span style={{ color: '#ef4444', fontSize: '12px' }}>{erros.email}</span>
          )}
        </div>

        <div style={{ marginBottom: '15px' }}>
          <label style={{ display: 'block', marginBottom: '5px', fontWeight: 'bold' }}>
            Senha:
          </label>
          <input
            type="password"
            value={senha}
            onChange={(e) => setSenha(e.target.value)}
            style={{
              width: '100%',
              padding: '10px',
              border: erros.senha ? '2px solid #ef4444' : '1px solid #d1d5db',
              borderRadius: '6px'
            }}
          />
          {erros.senha && (
            <span style={{ color: '#ef4444', fontSize: '12px' }}>{erros.senha}</span>
          )}
        </div>

        <button
          type="submit"
          style={{
            width: '100%',
            padding: '12px',
            backgroundColor: '#3b82f6',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            cursor: 'pointer',
            fontWeight: 'bold'
          }}
        >
          Cadastrar
        </button>
      </form>
    </div>
  );
}`,
  tags: ['validação', 'formulário', 'avançado', 'form']
},

{
  id: 'ex13',
  moduleId: 'fase2',
  title: "Accordion/FAQ",
  difficulty: 2,
  estimatedTime: "20 min",
  instruction: "O accordion já funciona! Melhore: adicione animação de entrada (transition), mude o ícone para + e -, ou adicione mais perguntas ao array.",
  hint: "Para animação suave, use CSS transition: 'all 0.3s ease'",
  initialCode: `function Accordion() {
  const [abaAberta, setAbaAberta] = React.useState(null);

  const perguntas = [
    {
      id: 1,
      pergunta: 'O que é JavaScript?',
      resposta: 'JavaScript é uma linguagem de programação que torna as páginas web interativas.'
    },
    {
      id: 2,
      pergunta: 'O que é React?',
      resposta: 'React é uma biblioteca JavaScript para construir interfaces de usuário.'
    },
    {
      id: 3,
      pergunta: 'O que é uma variável?',
      resposta: 'Uma variável é um espaço na memória para armazenar dados.'
    }
  ];

  const toggleAba = (id) => {
    setAbaAberta(abaAberta === id ? null : id);
  };

  return (
    <div style={{ 
      padding: '20px', 
      maxWidth: '600px',
      fontFamily: 'sans-serif'
    }}>
      <h3>❓ Perguntas Frequentes</h3>
      {perguntas.map((item) => (
        <div 
          key={item.id}
          style={{
            marginBottom: '10px',
            border: '1px solid #e5e7eb',
            borderRadius: '8px',
            overflow: 'hidden'
          }}
        >
          <button
            onClick={() => toggleAba(item.id)}
            style={{
              width: '100%',
              padding: '15px',
              backgroundColor: abaAberta === item.id ? '#3b82f6' : '#f3f4f6',
              color: abaAberta === item.id ? 'white' : '#1f2937',
              border: 'none',
              textAlign: 'left',
              cursor: 'pointer',
              fontWeight: 'bold',
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center'
            }}
          >
            {item.pergunta}
            <span>{abaAberta === item.id ? '▲' : '▼'}</span>
          </button>
          {abaAberta === item.id && (
            <div style={{
              padding: '15px',
              backgroundColor: 'white',
              borderTop: '1px solid #e5e7eb'
            }}>
              {item.resposta}
            </div>
          )}
        </div>
      ))}
    </div>
  );
}`
  tags: ['accordion', 'toggle', 'FAQ', 'intermediário']
}

// Exportar no final do arquivo exercises.js
export const exercisesData = [
  // ... exercícios da fase 1 ...
  // ... adicionar os exercícios acima ...
];
// Atualização da Fase 2 em src/data/courseData.jsx

{
  id: 'fase2',
  title: 'Fase 2: A Lógica (JavaScript)',
  icon: <Cpu className="w-6 h-6" />,
  color: 'bg-yellow-500',
  duration: '4-6 Semanas',
  description: 'A inteligência do site. Domine a linguagem antes do framework.',
  
  topics: [
    { id: 'f2-t1', text: 'Lógica: Variáveis, Condicionais e Loops' },
    { id: 'f2-t2', text: 'DOM: Manipulação de Elementos' },
    { id: 'f2-t3', text: 'ES6+: Arrow Functions e Destructuring' },
    { id: 'f2-t4', text: 'Arrays: Map, Filter e Reduce' }
  ],
  
  resources: [
    { 
      title: 'Curso Lógica de Programação (Guanabara)', 
      url: 'https://www.youtube.com/playlist?list=PLHz_AreHm4dlsK3Nr9GVvXCbpQyHQl1o1', 
      type: 'youtube',
      duration: '~40 aulas',
      level: 'Iniciante'
    },
    { 
      title: 'Curso JavaScript (Guanabara)', 
      url: 'https://www.youtube.com/playlist?list=PLntvgXM11X6pi7mW0O4ZmfUI1xDSIbmTm', 
      type: 'youtube',
      duration: '~60 aulas',
      level: 'Iniciante/Intermediário'
    },
    { 
      title: 'Map, Filter e Reduce (Rocketseat)', 
      url: 'https://www.youtube.com/watch?v=JbzcLKiTThk', 
      type: 'youtube',
      duration: '15 min',
      level: 'Intermediário'
    },
    { 
      title: 'JavaScript Moderno ES6+ (Willian Justen)', 
      url: 'https://www.youtube.com/watch?v=VHRdSnJbNLg', 
      type: 'youtube',
      duration: '30 min',
      level: 'Intermediário'
    },
    { 
      title: 'Async/Await Descomplicado (Rocketseat)', 
      url: 'https://www.youtube.com/watch?v=7Bs4-rqbCQc', 
      type: 'youtube',
      duration: '12 min',
      level: 'Intermediário'
    },
    { 
      title: 'Manipulação de DOM (Curso em Vídeo)', 
      url: 'https://www.youtube.com/watch?v=WWZX8RWLxIk', 
      type: 'youtube',
      duration: '25 min',
      level: 'Iniciante'
    },
    { 
      title: 'JavaScript Quiz (W3Schools)', 
      url: 'https://www.w3schools.com/js/js_quiz.asp', 
      type: 'game',
      description: 'Teste seus conhecimentos de JavaScript'
    },
    { 
      title: 'MDN JavaScript Guide', 
      url: 'https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Guide', 
      type: 'docs',
      description: 'Documentação oficial mais completa'
    }
  ],
  
  miniProject: {
    title: 'Calculadora Completa',
    description: 'Construa uma calculadora funcional com operações básicas (+, -, ×, ÷), histórico de cálculos e design profissional.',
    difficulty: 'Intermediário',
    estimatedTime: '1 semana',
    skills: ['DOM manipulation', 'Event handlers', 'State management', 'Math operations']
  },
  
  challenge: { 
    title: 'Calculadora IMC Avançada', 
    description: 'Crie uma calculadora de IMC completa com validações, feedback visual colorido, histórico de medições, gráfico de evolução e design responsivo.',
    difficulty: 'Intermediário',
    requirements: [
      'Validação de campos (peso 20-300kg, altura 0.5-2.5m)',
      'Feedback visual com cores e emojis',
      'Tabela de classificação IMC',
      'Histórico de medições',
      'Design responsivo (mobile-first)',
      'Cálculo de peso ideal'
    ]
  }
}
